#include "main.h"
/**
 * main - Entry point
 * Decription: 'points _putchar to stdout'
 * Return: always 0
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (1);
	}
	else
	{
		return (i * -1);
	}
	_putchar('\n');
}
