#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9).
 * @c: Returns 1 if c is a digit or Returns 0 otherwise
 * Return: always 0
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
	return (0);
}
